using CloneSteam.Core.Icons;
using System.Windows;
using System.Windows.Controls;

namespace CloneSteam.Library.UI.Units.Detail
{
    public class TopSubButton : Button
    {
        static TopSubButton()
        {
            DefaultStyleKeyProperty.OverrideMetadata(typeof(TopSubButton), new FrameworkPropertyMetadata(typeof(TopSubButton)));
        }

    
    }
}
